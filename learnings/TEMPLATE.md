---
name: short-slug
description: One-line summary, read by AI tools at session start for relevance ranking
applies: code-style
severity: preference
learned: YYYY-MM-DD
occurrences: 1
status: learning
---

# Title (verb phrase: "Prefer X over Y", "Avoid Z", etc.)

## The pattern

What's the rule? Show a code example if it makes the rule clearer.

## Why

The non-obvious reason James prefers this. Bullet points are fine.

If there's no non-obvious reason, this learning probably shouldn't exist —
see the anti-bloat checklist in `../CONTRIBUTING.md`.

## When this came up

The specific redirect — what was asked, what was done, what was corrected.
Keep it short, but specific enough that the *why* above is grounded in a
real moment, not a hypothetical.

## When NOT to apply

Edge cases or contexts where this rule doesn't hold. Always fill this in —
forcing yourself to think about scope is part of how this repo stays
honest.
